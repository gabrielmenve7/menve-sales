/**
 * Painel de resultados da aba Lista (filtros, enriquecimento, cards).
 * A lógica permanece em `pesquisa-client.tsx`; este módulo documenta o bloco `#lista-results`.
 */
export { PesquisaClient as ListaResultsHost } from "@/app/(dashboard)/pesquisa/pesquisa-client";
