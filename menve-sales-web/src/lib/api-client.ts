/**
 * @deprecated Use `apiServer` from `@/lib/api-server` (nome alinhado ao uso em Server Components/Actions).
 * Alias mantido para compatibilidade com o plano de arquitetura (`apiFetch`).
 */
export { apiServer as apiFetch, apiServerText } from "./api-server";
